---
import type { Lang } from '../../i18n/ui';
import { t } from '../../i18n/ui';
import { config } from '../../config';
import Container from '../shared/Container.astro';
import SectionHeading from '../shared/SectionHeading.astro';
import OpeningHours from '../shared/OpeningHours.astro';
import CTAButton from '../shared/CTAButton.astro';
import MapEmbed from '../shared/MapEmbed.astro';

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const contactHref = lang === 'da' ? '/kontakt' : '/en/contact';
---

<section id="hours" class="bg-cream pt-24 md:pt-40">
  <Container>
    <SectionHeading title={t(lang, 'home.location.title')} />

    <div data-animate-on-scroll class="max-w-2xl mx-auto text-center mb-16 opacity-0 translate-y-[30px] transition-all duration-[800ms] ease-out">
      <div class="space-y-6">
        <div>
          <h3 class="font-body text-xs uppercase tracking-[0.2em] text-ink-muted mb-3">
            {t(lang, 'contact.address')}
          </h3>
          <p class="font-display text-2xl text-ink">
            {config.address.street}
          </p>
          <p class="font-body font-light text-ink-light mt-1">
            {config.address.zip} {config.address.city}
          </p>
        </div>

        <div class="w-12 h-px bg-deep-red/20 mx-auto"></div>

        <OpeningHours lang={lang} compact={false} />

        <div class="pt-4">
          <CTAButton
            href={contactHref}
            variant="primary"
            label={t(lang, 'hero.cta.visit')}
          />
        </div>
      </div>
    </div>

    <a
      data-animate-on-scroll
      href="https://europeancoffeetrip.com/cafe/mors-aarhus/"
      target="_blank"
      rel="noopener noreferrer"
      class="group flex items-center justify-between gap-4 max-w-sm mx-auto mb-16 px-5 py-3.5 rounded-mors bg-[#1c0fb9] opacity-0 translate-y-[30px] transition-all duration-[800ms] ease-out hover:bg-[#1709a3] hover:-translate-y-[1px] hover:shadow-lg"
    >
      <img
        src="/brand/partners/ect-logo.png"
        alt="European Coffee Trip"
        width="276"
        height="82"
        class="h-6 w-auto"
        loading="lazy"
      />
      <span class="inline-flex items-center gap-2.5">
        <span class="font-body text-[10px] uppercase tracking-[0.22em] text-white/70">
          {lang === 'da' ? 'Anbefalet på' : 'Featured on'}
        </span>
        <svg viewBox="0 0 14 14" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" class="w-4 h-4 text-white/70 transition-transform duration-300 group-hover:translate-x-1 group-hover:text-white">
          <line x1="2" y1="7" x2="12" y2="7" />
          <polyline points="8,3 12,7 8,11" />
        </svg>
      </span>
    </a>
  </Container>

  <!-- Full-width map -->
  <div data-animate-on-scroll class="w-full h-[400px] md:h-[500px] opacity-0 translate-y-[30px] transition-all duration-[800ms] ease-out">
    <MapEmbed lang={lang} />
  </div>
</section>
