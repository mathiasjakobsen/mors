---
interface Props {
  title: string;
  subtitle?: string;
}

const { title, subtitle } = Astro.props;
---

<div data-animate-on-scroll class="text-center mb-16 md:mb-20 opacity-0 translate-y-[30px] transition-all duration-[800ms] ease-out">
  <h2 class="font-display font-bold text-4xl md:text-5xl text-deep-red mb-4">
    {title}
  </h2>
  {subtitle && (
    <p class="font-body font-light text-ink-light text-lg max-w-xl mx-auto leading-relaxed">
      {subtitle}
    </p>
  )}
</div>
